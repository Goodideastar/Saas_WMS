<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wms.mapper.WarehouseMapper">

    <resultMap id="BaseResultMap" type="com.wms.entity.Warehouse">
        <id column="id" property="id"/>
        <result column="warehouse_code" property="warehouseCode"/>
        <result column="warehouse_name" property="warehouseName"/>
        <result column="location" property="location"/>
        <result column="contact_person" property="contactPerson"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="version" property="version"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, warehouse_code, warehouse_name, location, contact_person, contact_phone,
        status, remark, create_time, update_time, create_by, update_by, is_deleted, version
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM warehouse WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM warehouse
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (warehouse_code LIKE CONCAT('%', #{keyword}, '%')
                 OR warehouse_name LIKE CONCAT('%', #{keyword}, '%')
                 OR location LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectCount" resultType="int">
        SELECT COUNT(*)
        FROM warehouse
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (warehouse_code LIKE CONCAT('%', #{keyword}, '%')
                 OR warehouse_name LIKE CONCAT('%', #{keyword}, '%')
                 OR location LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <select id="selectAllEnabled" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM warehouse
        WHERE is_deleted = 0 AND status = 1
        ORDER BY warehouse_code
    </select>

    <select id="countByCode" resultType="int">
        SELECT COUNT(*)
        FROM warehouse
        WHERE is_deleted = 0 AND warehouse_code = #{warehouseCode}
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
    </select>

    <insert id="insert" parameterType="com.wms.entity.Warehouse" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO warehouse (warehouse_code, warehouse_name, location, contact_person, contact_phone,
                               status, remark, create_by, update_by)
        VALUES (#{warehouseCode}, #{warehouseName}, #{location}, #{contactPerson}, #{contactPhone},
                #{status}, #{remark}, #{createBy}, #{updateBy})
    </insert>

    <update id="updateById" parameterType="com.wms.entity.Warehouse">
        UPDATE warehouse
        SET warehouse_name = #{warehouseName},
            location       = #{location},
            contact_person = #{contactPerson},
            contact_phone  = #{contactPhone},
            status         = #{status},
            remark         = #{remark},
            update_by      = #{updateBy},
            version        = version + 1
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE warehouse
        SET is_deleted = 1, update_by = #{updateBy}
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <select id="countOrdersByWarehouseId" resultType="int">
        SELECT
            (SELECT COUNT(*) FROM inbound_order WHERE warehouse_id = #{warehouseId} AND is_deleted = 0)
            +
            (SELECT COUNT(*) FROM outbound_order WHERE warehouse_id = #{warehouseId} AND is_deleted = 0)
    </select>

</mapper>
